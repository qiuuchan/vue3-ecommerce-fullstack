# src 文件夹说明

这个文件夹是“前端源码总仓库”，你写的页面、组件、路由、状态、接口工具，基本都在这里。

- `views/`：放页面（一整个页面那种）
- `components/`：放可复用组件（很多页面都能用）
- `router/`：放页面跳转规则（地址对应哪个页面）
- `store/`：放全局共享数据（比如购物车数量、登录信息）
- `api/`：放接口请求相关代码（统一处理请求失败）
- `types/`：放数据类型定义（先把数据结构写清楚）
